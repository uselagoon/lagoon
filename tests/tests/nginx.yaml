---

- include: features/api-token.yaml
  vars:
    testname: "API TOKEN"

- include: nginx/nginx.yaml
  vars:
    testname: "NGINX"
    git_repo_name: nginx.git
    project: ci-nginx
    branch: nginx
