---

- include: features/api-token.yaml
  vars:
    testname: "API TOKEN"

- include: nginx/nginx-kubernetes.yaml
  vars:
    testname: "NGINX KUBERNETES"
    git_repo_name: nginx.git
    project: ci-nginx-kubernetes
    branch: nginx
