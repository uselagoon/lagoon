---

- include: features/api-token.yaml
  vars:
    testname: "API TOKEN"

- include: drupal/drupal.yaml
  vars:
    testname: "Drupal 8 composer PHP 7.2 - POSTGRES SINGLE {{ lookup('env','CLUSTER_TYPE')|upper }}"
    drupal_version: 8
    db: postgres-single
    php_version: 7.2
    git_repo_name: drupal-postgres-single.git
    project: ci-drupal-postgres-single-{{ lookup('env','CLUSTER_TYPE') }}
    branch: drupal8-composer-72-postgres-single
