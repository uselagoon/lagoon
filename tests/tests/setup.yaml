---
- name: "Setup test data for entire test suite"
  hosts: localhost
  serial: 1
  tasks:
  # - include: setup/deploy-targets.yaml.yaml
  #   vars:
  #     testname: "Add Deploy Targets"

- ansible.builtin.import_playbook: setup/groups.yaml
  vars:
    testname: "Add Groups"

- ansible.builtin.import_playbook: setup/users.yaml
  vars:
    testname: "Add Users"

- ansible.builtin.import_playbook: setup/users-to-group.yaml
  vars:
    testname: "Add Users to Groups"

- ansible.builtin.import_playbook: setup/notifications.yaml
  vars:
    testname: "Add Notificationss"
