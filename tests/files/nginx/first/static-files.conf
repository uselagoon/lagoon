server {

    listen 8080 default_server;

    include /etc/nginx/helpers/*.conf;

    location / {
        echo_sleep 1.0; # 1 sec delay
        index index.html index.htm;
        try_files $uri $uri/ =404;
    }

}