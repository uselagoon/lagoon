version: '2'
services:
  nginx:
    networks:
      - amazeeio-network
      - default
    image: amazeeio/nginx
    labels:
      lagoon.type: nginx
    volumes:
      - ./app:/app:delegated
    ports:
      - "8080:8080"
    environment:
      - AMAZEEIO_URL=nginx.docker.amazee.io
networks:
  amazeeio-network:
    external: true