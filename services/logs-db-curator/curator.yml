---
client:
  hosts:
    - ${ELASTICSEARCH_HOST}
  port: ${ELASTICSEARCH_HOST_PORT}
  http_auth: ${LOGSDB_HTTP_AUTH}
  use_ssl: ${LOGSDB_USE_SSL}

logging:
  loglevel: INFO
  logfile:
  logformat: default
  blacklist: ['elasticsearch', 'urllib3']