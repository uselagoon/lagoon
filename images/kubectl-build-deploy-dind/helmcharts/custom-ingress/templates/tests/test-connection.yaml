apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "custom-ingress.fullname" . }}-test-connection"
  labels:
{{ include "custom-ingress.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "custom-ingress.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
