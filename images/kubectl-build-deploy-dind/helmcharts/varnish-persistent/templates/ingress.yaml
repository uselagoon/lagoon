{{- if .Values.routesAutogenerateEnabled -}}
{{- $fullName := include "varnish-persistent.fullname" . -}}
{{- $host := include "varnish-persistent.autogeneratedHost" . -}}
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: {{ $fullName }}
  labels:
    lagoon/autogenerated: "true"
    {{- include "varnish-persistent.labels" . | nindent 4 }}
  annotations:
    {{- include "varnish-persistent.annotations" . | nindent 4 }}
  {{- with .Values.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  tls:
    - hosts:
        -   {{ $host | quote }}
  rules:
    - host: {{ $host | quote }}
      http:
        paths:
          - backend:
              serviceName: {{ $fullName }}
              servicePort: {{ .Values.service.port }}
{{- end }}
