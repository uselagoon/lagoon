apiVersion: networking.k8s.io/v1beta1
kind: Ingress
# data:
#   hsts: {{ .Values.route_hsts }}
#   hsts-max-age: {{ .Values.route_hsts }}
#   ssl-redirect: true
metadata:
  name: {{ .Values.route_domain }}
  labels:
    lagoon/autogenerated: "true"
  annotations:
    # force-ssl-redirect handling
    {{ if eq .Values.route_insecure "Allow"}}
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    {{ else if eq .Values.route_insecure "Redirect"}}
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    {{ else if eq .Values.route_insecure "None"}}
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    {{ end }}
    # HSTS Handling
    {{- if .Values.route_hsts}}
    # haproxy.router.openshift.io/hsts_header: {{ .Values.route_hsts }}
    {{ end -}}
    kubernetes.io/tls-acme: {{ .Values.route_tls_acme | quote }}
spec:
  {{if ne .Values.route_insecure "None"}}
  tls:
    - hosts:
        -  {{ .Values.route_domain }}
  {{ end}}
  rules:
    - host: {{ .Values.route_domain }}
      http:
        paths:
          - backend:
              serviceName: {{ .Values.route_service }}
              servicePort: http


