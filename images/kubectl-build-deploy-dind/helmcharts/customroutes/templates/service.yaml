apiVersion: v1
kind: Service
metadata:
  name: {{ include "customroutes.fullname" . }}
  labels:
    {{- include "customroutes.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "customroutes.selectorLabels" . | nindent 4 }}
