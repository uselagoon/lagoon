{{- if .Values.routesAutogenerateEnabled -}}
{{- $fullName := include "nginx-php.fullname" . -}}
{{- $host := include "nginx-php.autogeneratedHost" . -}}
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: {{ $fullName }}
  labels:
    lagoon/autogenerated: "true"
    {{- include "nginx-php.labels" . | nindent 4 }}
  annotations:
    {{- include "nginx-php.annotations" . | nindent 4 }}
spec:
  rules:
    - host: {{ $host | quote }}
      http:
        paths:
          - backend:
              serviceName: {{ $fullName }}
              servicePort: {{ .Values.service.port }}
{{- end }}
